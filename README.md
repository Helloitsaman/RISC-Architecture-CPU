So far built the ALU & Register File so computation and memory storing, retrieving data from memory is completed
Yet To Build : Control Unit and idk what will come after that, maybe scale to 64 bit as well since its 32 bit so far
